{
  "name": "collab-tasks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix server run build",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
